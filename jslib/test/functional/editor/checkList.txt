モデルエディターのGUIの機能テストのチェックリスト。
jsunitでテストできない。GUIイベントのチェック。


DOM操作関連
	コンテナ作成
		centerTcに配置可能か。
		centerTcからタイトルのxボタンをクリックで削除可能か。
		projectExploerからダブルクリックで再表示可能か。

		
	

	コンポーネント配置
		toolboxからD&D可能か。
		propertyViewに表示されているか。
		eventTcにイベントリストが表示されているか。
		ドラッグで移動可能か。
		ドラッグでリサイズ可能か。
		propertyViewの変更が、centerTc上のコンポーネントに反映されているか。
		centerTcでのコンポーネントへの変更が、propertyViewに反映されているか。
		削除ボタンから削除可能か。
			イベント、プロパティも同時に削除されているか。
	
	メニュー
	
